# 全局配置
{
    admin :2019 {
	
    }
    # 全局选项，例如日志配置
    log {
        output stdout  # 输出到标准输出
        format json {
            level_format upper
        }
        format console # 使用 console 格式
        #format console {
        #    message_key "msg"
        #    level_key "level"
        #    time_key "ts"
        #    name_key "name"
        #    caller_key "caller"
        #    stacktrace_key "stacktrace"
        #    #line_ending \n
        #    time_format "2006-01-02 15:04:05.000"
        #    duration_format "seconds"
        #    level_format "color"
        #}
        
        level DEBUG    # 设置日志级别（DEBUG, INFO, WARN, ERROR 等）
        
    }
}

# 引用其他的caddy配置文件
import /etc/caddy/caddy.d/*.caddy

